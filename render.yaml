services:
  - type: web
    name: newsr-inventory-api
    env: node
    plan: free
    rootDir: .
    buildCommand: npm install
    startCommand: npm run api
    healthCheckPath: /api/v1/health
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: PORT
        value: 4000
